name: Auto-assign issues to product owners

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    uses: OpenAEC-Foundation/conventions/.github/workflows/auto-assign-issues.yml@main
    with:
      assignees: '["DutchSailor","mojtabakarimi"]'
